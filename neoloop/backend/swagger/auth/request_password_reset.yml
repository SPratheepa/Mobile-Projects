tags:
  - Authentication

summary: Request Password Reset

parameters:
  - in: body
    name: body
    required: true

    schema:
      type: object

      required:
        - email

      properties:

        email:
          type: string
          example: sree@test.com

responses:

  200:
    description: Reset token generated