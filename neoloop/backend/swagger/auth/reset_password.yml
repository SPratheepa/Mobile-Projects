tags:
  - Authentication

summary: Reset Password

parameters:
  - in: body
    name: body
    required: true

    schema:
      type: object

      required:
        - token
        - new_password

      properties:

        token:
          type: string

        new_password:
          type: string
          example: Password@123

responses:

  200:
    description: Password reset successful