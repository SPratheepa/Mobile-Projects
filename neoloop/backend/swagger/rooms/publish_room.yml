tags:
  - Rooms

summary: Publish Room

description: Publish a room.

security:
  - BearerAuth: []

parameters:
  - in: path
    name: room_id
    required: true
    schema:
      type: integer

responses:
  200:
    description: Room published successfully

  404:
    description: Room not found

  403:
    description: Permission denied